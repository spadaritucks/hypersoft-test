export type ProductsResponseDTO = {
    id: string;
    name: string;
    description: string;
    price: number;
    categoryId: string;
    stockQuantity: number;
    createdAt: string;
    updatedAt: string;
}
